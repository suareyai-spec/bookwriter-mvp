# 2026-03-04

## KOSIQ Updates (Mar 3 evening)
- Renamed "Action Center" to "AtlasIQ Dashboard"
- ClinIQ Tree Builder: clickable nodes show patient lists (up to 100), drag-and-drop re-parenting, drag vs click detection (3px threshold)
- ClinIQ Templates: "Use Template" opens builder modal with full tree visualization, "Load into Builder" navigates to builder page and loads template via localStorage, PDF download per template
- Reusable `PatientDrillDown` component created at `src/components/PatientDrillDown.tsx` — used across all 15 product pages
- All patient counts/population numbers across all products are now clickable drill-downs (searchable, sortable, copy, PDF export)
- Export buttons on ClinIQ changed from JSON to branded PDF (jsPDF)
- Modal prop fix: component uses `open` not `isOpen` — was causing modals not to open
- Investor pitch deck updated to 19 slides (was 18): new 11-product grid, platform architecture diagram, 5 structural advantages
- Signup page: 2-step flow (personal info → org join/create), invite codes, role selection
- Settings page: 4 tabs (Profile, Security, Notifications, Appearance)
- KOSIQ logo moved to bottom of sidebar (non-clickable when signed in)
- ChartIQ fully migrated into KOSIQ platform (sub-agent completed)
- Role-based access control: Provider/Payer/Enterprise bundles, org management, superadmin override
- GitHub pushed multiple times (commit 4db985b with 33 files, 3079 lines added)

## ComfyUI Setup (Mar 5)
- David wants to set up ComfyUI for NSFW image generation
- Pipeline: Higgsfield (create character model) → ComfyUI (IP-Adapter FaceID for face consistency + NSFW generation + body-part-specific upscaling)
- Key components: Pony Diffusion V6 or RealVisXL, IP-Adapter FaceID Plus V2, UltimateSDUpscale, FaceDetailer, ControlNet Tile, 4x-UltraSharp
- David accidentally installed Homebrew on the VPS instead of Mac Mini — redirected to run on Mac
- Will create private GitHub repo for custom workflow files
- Mac Mini hostname: Davids-Mac-mini-2
